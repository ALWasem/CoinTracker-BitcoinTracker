## Copy to .env and edit as needed

# Use Postgres (otherwise the app defaults to SQLite at Backend/cointracker.db)
# DATABASE_URL=postgresql://coinuser:coinpass@localhost/CoinTrackerBitcoinTracker

# If using an API that requires a key, set it here and read in services/blockchain_api.py
# BLOCKCHAIN_API_KEY=

